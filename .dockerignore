# Keep the build context as small as possible by whitelisting needed files.
# Exclude everything by default.
*

# Whitelist only files needed in the build context.
!package.json
!pnpm-lock.yaml
!tsconfig.json
!src
